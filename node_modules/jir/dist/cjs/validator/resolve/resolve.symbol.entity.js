"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.resolveEntitySymbol = resolveEntitySymbol;
/* eslint-disable @typescript-eslint/no-explicit-any */
const chars_1 = require("../chars");
const entity_operations_1 = require("../entity.operations");
const parse_type_1 = require("../parse-type");
const source_types_1 = require("../source-types");
const symbols_1 = require("../symbols");
const entity_1 = require("../symbols/entity");
const visibility_1 = require("../visibility");
const resolve_name_1 = require("./resolve.name");
const resolve_symbol_1 = require("./resolve.symbol");
const resolve_symbol_from_schema_1 = require("./resolve.symbol.from-schema");
function resolveEntitySymbol(_symbol, context) {
    const symbol = (0, resolve_symbol_from_schema_1.resolveSymbolFromSchema)(_symbol, entity_1.RawEntitySymbolSchema.omit({
        properties: true,
    }));
    const entity = { ...symbol, properties: {} };
    const { raw } = _symbol;
    if (!raw || typeof raw !== "object" || !("properties" in raw)) {
        throw new Error(`Object '${symbol.name}' is missing 'properties'`);
    }
    const properties = raw.properties;
    if (!(properties && typeof properties === "object")) {
        throw new Error(`Object '${symbol.name}' must have a valid 'properties' object`);
    }
    Object.keys(properties).forEach((nameOfProperty) => {
        if (!nameOfProperty ||
            !nameOfProperty.split(" ").every((ch) => (0, chars_1.isAlnum)(ch) || ch === "_")) {
            throw new Error(`Object '${symbol.name}' property '${nameOfProperty}' must be made of alphabets and or digits`);
        }
        if (nameOfProperty[0] !== "_" && !(0, chars_1.isAlpha)(nameOfProperty[0])) {
            throw new Error(`Object '${symbol.name}' property '${nameOfProperty}' must start with an alphabet or '_'`);
        }
        const property = properties[nameOfProperty];
        const displayName = `${symbol.name}.${nameOfProperty}`;
        if (nameOfProperty === "id") {
            throw new Error(`On '${entity.name}', entities have 'id' fields autogenerated. You may not define them manually`);
        }
        const expandedProperty = typeof property !== "string"
            ? (() => {
                if (!("type" in property)) {
                    throw new Error(`Object '${symbol.name}' property '${nameOfProperty}' has an unknown type`);
                }
                return {
                    ...property,
                    ...(0, parse_type_1.parseFieldType)(`${displayName}$type`, property.type),
                };
            })()
            : (0, parse_type_1.parseFieldType)(displayName, property);
        const type = (0, source_types_1.parseTypeFromSource)(expandedProperty);
        if (type === symbols_1.SymbolKind.Entity) {
            throw new Error(`type '${property.type}' not allowed for '${displayName}'`);
        }
        if ("visibility" in expandedProperty &&
            !visibility_1.visibilities.includes(expandedProperty.visibility)) {
            throw new Error(`'${displayName}' has unknown visibility '${expandedProperty.visibility}'`);
        }
        const propertyAsSymbol = {
            name: displayName,
            referenceCount: 0,
            raw: expandedProperty,
            status: symbols_1.SymbolStatus.Unresolved,
            type,
            visibility: expandedProperty.visibility || "inherit",
        };
        const resolved = (0, resolve_symbol_1.resolveSymbol)(propertyAsSymbol, context);
        entity.properties[nameOfProperty] = resolved;
        if (resolved.abstract) {
            throw new Error(`Abstract type '${type}' not allowed for '${displayName}'`);
        }
        if ("reference" in resolved && resolved.reference) {
            throw new Error(`Object property '${displayName}' must not reference other entities/objects`);
        }
        // @todo: figure out how we're gonna deal with reflexive relations and necessary cyclic dependencies
    });
    entity.properties.id = (0, resolve_symbol_1.resolveSymbol)({
        name: `${entity.name}.id`,
        referenceCount: 0,
        raw: {
            type: context.target === "mongodb" ? "string" : "number",
            objectId: context.target === "mongodb",
            id: true,
            autoincrement: context.target !== "mongodb",
            default: context.target === "mongodb" ? "auto()" : "@autoincrement()",
        },
        visibility: "public",
        status: symbols_1.SymbolStatus.Unresolved,
        type: context.target === "mongodb" ? symbols_1.SymbolKind.Text : symbols_1.SymbolKind.Number,
    }, context);
    console.log(raw);
    if ("+operations" in raw) {
        const { data: operations, success } = entity_operations_1.EntityOperationsSchema.safeParse(raw["+operations"]);
        if (!success) {
            throw new Error(`Invalid operations spec '${symbol.name}.operations'`);
        }
        entity.operations = [
            ...new Set([...(symbol.operations || []), ...operations]),
        ];
    }
    if ("-operations" in raw) {
        const { data: operations, success } = entity_operations_1.EntityOperationsSchema.safeParse(raw["-operations"]);
        if (!success) {
            throw new Error(`Invalid operations spec '${symbol.name}.operations'`);
        }
        entity.operations = (symbol.operations || []).filter((one) => !operations.includes(one));
    }
    if (symbol.permissions) {
        const permissions = symbol.permissions;
        for (const [userKind, validOperations] of Object.entries(permissions)) {
            for (const operation of validOperations) {
                if (!(symbol.operations || []).includes(operation)) {
                    throw new Error(`Unknown operation '${operation}' specified in '${symbol.name}.permissions' for role '${userKind}'`);
                }
            }
        }
    }
    if (symbol.extends) {
        const parents = typeof symbol.extends === "string" ? [symbol.extends] : symbol.extends;
        for (const proposedParent of parents) {
            const parent = (0, resolve_name_1.resolveName)(proposedParent, context);
            if (parent.final) {
                throw new Error(`'${symbol.name}' cannot extend '${proposedParent}' as that guy is marked 'final'`);
            }
            if (![symbols_1.SymbolKind.Entity, symbols_1.SymbolKind.Object].includes(parent.type)) {
                throw new Error(`'${symbol.name}' cannot extend a non-entity/object type '${proposedParent}'`);
            }
            if (!("properties" in parent)) {
                throw new Error(`'${symbol.name}' is extending a type with missing properties '${proposedParent}'`);
            }
            entity.properties = {
                ...parent.properties, // we spread them first so that they dont overwrite preexisting ones
                ...entity.properties,
            };
        }
    }
    if (entity.compositeKey) {
        for (const id of entity.compositeKey) {
            if (!(id in entity.properties)) {
                throw new Error(`'${entity.name}.compositeKey' has field name '${id}' which doesn't exist in the entity`);
            }
        }
    }
    if (entity.uniqueKey) {
        const uniqueKey = Array.isArray(entity.uniqueKey)
            ? {
                fields: entity.uniqueKey,
            }
            : entity.uniqueKey;
        for (const field of uniqueKey.fields) {
            if (!(field in entity.properties)) {
                throw new Error(`'${entity.name}.uniqueKey' has field name '${field}' which doesn't exist in the entity`);
            }
        }
    }
    if (entity.index) {
        const index = Array.isArray(entity.index)
            ? {
                fields: entity.index,
            }
            : entity.index;
        for (const field of index.fields) {
            if (!(field in entity.properties)) {
                throw new Error(`'${entity.name}.uniqueKey' has field name '${field}' which doesn't exist in the entity`);
            }
        }
    }
    return entity;
}
