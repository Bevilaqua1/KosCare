"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.resolveName = resolveName;
const resolve_symbol_1 = require("./resolve.symbol");
function resolveName(name, context) {
    if (!(name in context.table)) {
        throw new Error(`Referencing undefined name '${name}'`);
    }
    context.table[name] = (0, resolve_symbol_1.resolveSymbol)(context.table[name], context);
    context.table[name].referenceCount += 1;
    return context.table[name];
}
