"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.resolveListSymbol = resolveListSymbol;
const resolve_symbol_1 = require("./resolve.symbol");
const symbols_1 = require("../symbols");
const list_1 = require("../symbols/list");
const resolve_symbol_from_schema_1 = require("./resolve.symbol.from-schema");
function resolveListSymbol(_symbol, context) {
    const symbol = (0, resolve_symbol_from_schema_1.resolveSymbolFromSchema)(_symbol, list_1.RawListSymbolSchema.omit({
        of: true,
    }));
    const raw = _symbol.raw;
    if (!("of" in raw)) {
        throw new Error(`Found no base type while parsing '${symbol.name}'`);
    }
    const displayName = `${symbol.name}.0`;
    const base = raw.of;
    if (!base) {
        throw new Error(`Found no base type while parsing '${displayName}'. Base type: ${JSON.stringify(base)}`);
    }
    if (typeof base !== "string") {
        if (typeof base !== "object") {
            throw new Error(`Found invalid base type while parsing '${displayName}'. Base type: ${JSON.stringify(base)}`);
        }
        if (!("type" in base)) {
            throw new Error(`Found invalid base type (missing 'type') while parsing '${displayName}'`);
        }
    }
    const baseTypeSymbol = {
        name: displayName,
        referenceCount: 0,
        raw: base,
        status: symbols_1.SymbolStatus.Unresolved,
        visibility: "inherit",
        type: (() => {
            if (typeof base === "string") {
                return base;
            }
            switch (base.type) {
                case "entity": {
                    throw new Error(`type '${base.type}' not allowed for '${displayName}'`);
                }
                case "enum":
                    return symbols_1.SymbolKind.Enum;
                case "object":
                    return symbols_1.SymbolKind.Object;
                case "string":
                    return symbols_1.SymbolKind.Text;
                case "number":
                    return symbols_1.SymbolKind.Number;
                case "boolean":
                    return symbols_1.SymbolKind.Boolean;
                case "list":
                    return symbols_1.SymbolKind.List;
                default:
                    return base.type;
            }
        })(),
    };
    const list = symbol;
    list.of = (0, resolve_symbol_1.resolveSymbol)(baseTypeSymbol, context);
    if (list.of.type === symbols_1.SymbolKind.Entity) {
        throw new Error(`List '${symbol.name}' cannot have an underlying of type '${list.of.type}'`);
    }
    return list;
}
