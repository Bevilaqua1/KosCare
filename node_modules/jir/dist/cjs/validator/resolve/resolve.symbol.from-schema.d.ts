import { z, ZodSchema } from "zod";
import { Symbol } from "../symbols";
export declare function resolveSymbolFromSchema<Schema extends ZodSchema>(symbol: Symbol, schema: Schema): z.infer<Schema> & Symbol;
