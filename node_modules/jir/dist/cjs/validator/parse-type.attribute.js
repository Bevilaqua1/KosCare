"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.parseTypeAttributeName = parseTypeAttributeName;
exports.parseTypeAttributesList = parseTypeAttributesList;
exports.parseTypeAttributes = parseTypeAttributes;
/* eslint-disable @typescript-eslint/no-explicit-any */
const chars_1 = require("./chars");
const parse_type_attribute_booleans_1 = require("./parse-type.attribute.booleans");
const parse_type_attribute_dates_1 = require("./parse-type.attribute.dates");
const parse_type_attribute_numbers_1 = require("./parse-type.attribute.numbers");
const parse_type_attribute_strings_1 = require("./parse-type.attribute.strings");
const visibility_1 = require("./visibility");
function parseTypeAttributeName(ctx) {
    let attributeName = "";
    while (ctx.moreFood() && (0, chars_1.isAlnum)(ctx.taste())) {
        attributeName += ctx.swallow();
    }
    if (!attributeName.length) {
        throw new Error(`Found unnamed type attribute`);
    }
    return attributeName;
}
function parseTypeAttributesList(ctx, parse) {
    let result = {
        visibility: "inherit",
    };
    while (ctx.moreFood() && ctx.taste() === "@") {
        ctx.swallow();
        const attributeName = parseTypeAttributeName(ctx);
        if (result[attributeName]) {
            ctx.fatalError(`Duplicate attribute '${attributeName}'`);
        }
        if (visibility_1.visibilities.includes(attributeName)) {
            result.visibility = attributeName;
        }
        else if (["unique", "optional"].includes(attributeName)) {
            result[attributeName] = true;
        }
        else if (attributeName === "map") {
            result[attributeName] = ctx.swallowStringAttributeArgument(attributeName);
        }
        else {
            result = {
                ...result,
                ...parse(attributeName, ctx),
            };
        }
        ctx.swallowAir();
    }
    return result;
}
function parseTypeAttributes(type, ctx) {
    const parser = (() => {
        switch (type) {
            case "number":
                return parse_type_attribute_numbers_1.parseNumberTypeAttribute;
            case "string":
                return parse_type_attribute_strings_1.parseStringTypeAttribute;
            case "boolean":
                return parse_type_attribute_booleans_1.parseBooleanTypeAttribute;
            case "date":
                return parse_type_attribute_dates_1.parseDateTypeAttribute;
            default: {
                ctx.fatalError(`Attributes not allowed for type '${type}'`);
                throw new Error(`Unreachable`);
            }
        }
    })();
    return parseTypeAttributesList(ctx, parser);
}
