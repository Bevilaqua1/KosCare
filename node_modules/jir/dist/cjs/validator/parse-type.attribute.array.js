"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.listBooleanFlags = void 0;
exports.parseArrayTypeAttribute = parseArrayTypeAttribute;
/* eslint-disable @typescript-eslint/no-explicit-any */
const parse_type_attribute_1 = require("./parse-type.attribute");
const parse_type_attribute_booleans_1 = require("./parse-type.attribute.booleans");
const parse_type_attribute_dates_1 = require("./parse-type.attribute.dates");
const parse_type_attribute_numbers_1 = require("./parse-type.attribute.numbers");
const parse_type_attribute_strings_1 = require("./parse-type.attribute.strings");
function parseArrayTypeAttribute(attributeName, baseType, ctx) {
    if (exports.listBooleanFlags.includes(attributeName)) {
        return { [attributeName]: true };
    }
    const result = {};
    switch (attributeName) {
        case "max":
        case "min":
        case "length": {
            result[attributeName] = ctx.swallowNumberAttributeArgument(attributeName);
            break;
        }
        case "none":
        case "some":
        case "every": {
            ctx.expect("(", `Expected '(' for '@${attributeName}'`);
            let temp = {};
            if (baseType.type !== "list") {
                const parse = (() => {
                    switch (baseType.type) {
                        case "number":
                            return parse_type_attribute_numbers_1.parseNumberTypeAttribute;
                        case "string":
                            return parse_type_attribute_strings_1.parseStringTypeAttribute;
                        case "boolean":
                            return parse_type_attribute_booleans_1.parseBooleanTypeAttribute;
                        case "date":
                            return parse_type_attribute_dates_1.parseDateTypeAttribute;
                        default: {
                            ctx.fatalError(`Attributes not allowed for type '${baseType.type}'`);
                            throw new Error(`Unreachable`);
                        }
                    }
                })();
                do {
                    const name = (0, parse_type_attribute_1.parseTypeAttributeName)(ctx);
                    ctx.swallowAir();
                    if (temp[name]) {
                        ctx.fatalError(`Duplicate attribute '${name}'`);
                    }
                    temp = {
                        ...temp,
                        ...parse(name, ctx),
                    };
                    ctx.swallowAir();
                    if (ctx.tastesLike(",")) {
                        ctx.swallow();
                        ctx.swallowAir();
                        continue;
                    }
                    break;
                } while (!ctx.noMoreFood());
            }
            else {
                do {
                    const name = (0, parse_type_attribute_1.parseTypeAttributeName)(ctx);
                    ctx.swallowAir();
                    if (temp[name]) {
                        ctx.fatalError(`Duplicate attribute '${name}'`);
                    }
                    temp = {
                        ...temp,
                        ...parseArrayTypeAttribute(name, baseType.of, ctx),
                    };
                    ctx.swallowAir();
                    if (ctx.tastesLike(",")) {
                        ctx.swallow();
                        ctx.swallowAir();
                        continue;
                    }
                    break;
                } while (!ctx.noMoreFood());
            }
            result[attributeName] = temp;
            ctx.expect(")", `Expected ')' for '@${attributeName}', but found '${ctx.sample(4)}'`);
            break;
        }
        case "default":
        default: {
            ctx.fatalError(`Found impermissible attribute '@${attributeName}'`);
        }
    }
    return result;
}
exports.listBooleanFlags = ["empty", "nonempty"];
