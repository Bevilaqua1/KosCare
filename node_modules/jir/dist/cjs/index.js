"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
const fs = __importStar(require("fs"));
// import { resolve } from "./validator/resolve";
const validator_1 = require("./validator");
const resolve_1 = require("./validator/resolve");
// import { generateFromValidationContext } from "./prisma/generate/generate.from-context";
// === Read sample.json and call validate ===
if (require.main === module) {
    try {
        const data = fs.readFileSync("sample-ir/entities.json", "utf-8");
        const parsed = JSON.parse(data);
        if (!Array.isArray(parsed)) {
            throw new Error("Invalid input: Expected an array of declarations");
        }
        for (const item of parsed) {
            (0, validator_1.define)(item);
        }
        (0, resolve_1.resolve)(validator_1.context);
        console.log(JSON.stringify(validator_1.context.table, null, 2));
        // const temp = generateFromValidationContext(context, {});
        // console.log(temp);
    }
    catch (err) {
        console.error("Error:", err);
        process.exit(1);
    }
}
exports.default = {
// parse,
// generate,
// transform,
// compile,
};
